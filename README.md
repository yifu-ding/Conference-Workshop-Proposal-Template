# Conference Workshop Proposal Template

This repository contains a simple conference workshop proposal template in `conf_workshop_template.tex`. It is based on a successful proposal from previous workshops.

To use it, upload the file to Overleaf and compile with `pdflatex`.
